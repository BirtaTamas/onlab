# LSTM Feature Importance Figure Notes

## Global figures

- `feature_importance_overall_top_features.png`
- `feature_importance_utility_top_features.png`

## Suggested case-study figures

- case `60`: round `13`, top15 utility feature count `9`, strong utility rows `44`, max |LSTM-XGBoost| `0.292`
- case `29`: round `4`, top15 utility feature count `9`, strong utility rows `184`, max |LSTM-XGBoost| `0.090`
- case `01`: round `20`, top15 utility feature count `8`, strong utility rows `126`, max |LSTM-XGBoost| `0.177`
